# README Banner Prompt

Use this prompt to generate a README hero banner for LACP:

```text
Create a wide GitHub README hero banner (1600x500), dark graphite background with subtle grid texture, cinematic but clean.
Title text: “LACP” large, modern monospace + sans pairing.
Subtitle: “Local Agent Control Plane for Claude/Codex”.
Visual motif: layered control-plane diagram with nodes/edges for “onboard”, “verify”, “doctor”, “route”, “sandbox”, “memory”, “gates”, “artifacts”.
Show three gate badges in UI style: “risk-tier”, “approval TTL”, “budget ceiling”.
Add tiny terminal snippet block: `lacp-install --profile starter --with-verify` and `lacp-test --quick`.
Style: developer infrastructure aesthetic, high contrast, minimal clutter, no stock-photo look, no people, no mascots.
Color accents: electric cyan + safety amber + soft green on dark neutral palette.
Composition: centered title, right-side system graph, left-bottom command block.
Output must be crisp, readable at small size, and optimized for GitHub README display.
```
